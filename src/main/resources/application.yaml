server:
  port: 8080
  servlet:
    context-path: /

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      pathMapping:
        health: /health-check
      basePath: /